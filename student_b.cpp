#include "shared_types.h"

// Заглушка для Студента Б (Метод Ньютона)
std::unique_ptr<Result> calculateB(std::shared_ptr<const InputData> data) {
    auto res = std::make_unique<Result>();
    return res;
}